import 

class JobManager():

	def __init__(self):
		# create a new thread, and initialize the empty queue
		# have thread call a create_listener function which looks into the job
		# queue and attempts to make requests with the c job_manager
		
	def push_job(self, obj_id):
		# append a job into the queue if the object exists in the database
	
	def __create_listener(self):
		# create while loop which looks every second for a new job in the queue
